name: Build Android AAB

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build web app
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Generate Keystore
        run: |
          keytool -genkeypair \
            -keystore instacord.keystore \
            -alias instacord \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass InstaCord2024! \
            -keypass InstaCord2024! \
            -dname "CN=INSTA-CORD, OU=App, O=INSTA-CORD, L=US, ST=US, C=US"

      - name: Build & Sign Release AAB
        run: |
          cd android
          chmod +x gradlew
          ./gradlew bundleRelease \
            -Pandroid.injected.signing.store.file=$GITHUB_WORKSPACE/instacord.keystore \
            -Pandroid.injected.signing.store.password=InstaCord2024! \
            -Pandroid.injected.signing.key.alias=instacord \
            -Pandroid.injected.signing.key.password=InstaCord2024!

      - name: Upload Signed AAB
        uses: actions/upload-artifact@v4
        with:
          name: INSTA-CORD-release
          path: android/app/build/outputs/bundle/release/app-release.aab
          retention-days: 30
